import { useMemo, useState } from "react";
export default function TentUsage({ rows }) {
  const [sort, setSort] = useState("bed_nights");
  const visible = useMemo(() => [...rows].sort((a,b) => Number(b[sort] || 0) - Number(a[sort] || 0)).slice(0,10), [rows, sort]);
  return <section className="analytics-print-section rounded-xl border bg-card overflow-hidden"><div className="p-4 border-b flex justify-between"><div><h2 className="font-semibold">שימוש לפי אוהל</h2><p className="text-xs text-muted-foreground">10 האוהלים המובילים</p></div><select value={sort} onChange={e => setSort(e.target.value)} className="analytics-no-print rounded-md border bg-background px-2 text-sm"><option value="bed_nights">לינות אדם משובצות</option><option value="uses_count">שימושים</option><option value="occupied_nights">לילות תפוסים</option></select></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-muted/60"><tr>{["אוהל","שכונה","קיבולת","שימושים","סה״כ אדם-לילה משובץ","לילות תפוסים","לינות אדם משובצות","קבוצות","תפוסה"].map(x => <th key={x} className="p-3 text-right whitespace-nowrap">{x}</th>)}</tr></thead><tbody>{visible.map(row => <tr key={row.tent_id} className="border-t"><td className="p-3 font-medium">{row.tent_number}</td><td className="p-3">{row.neighborhood}</td><td className="p-3">{row.capacity}</td><td className="p-3">{row.uses_count}</td><td className="p-3">{row.allocated_pax}</td><td className="p-3">{row.occupied_nights}</td><td className="p-3">{row.bed_nights}</td><td className="p-3">{row.groups_count}</td><td className="p-3">{row.occupancy_rate?.toFixed(1)}%</td></tr>)}</tbody></table></div></section>;
}